node_modules
dist
.turbo
*.tsbuildinfo
.DS_Store
.env
.env.local
.vite
coverage
playwright-report
test-results

# local docker-compose overrides (e.g. alternate host ports for testing) —
# never intended to be committed
docker-compose.override.yml

# backend (Python)
backend/.venv
**/__pycache__
*.egg-info
.pytest_cache
