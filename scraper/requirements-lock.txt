# Reproducibility pin for `scraper/Dockerfile`'s build — same purpose as
# backend/requirements-lock.txt (see that file for the full rationale). Used
# as a `pip install --constraint` file: pins versions among what
# pyproject.toml already asks for, doesn't add packages, so the dev-only
# extras below never leak into the runtime image.
#
# Regenerate after a deliberate dependency bump:
#   cd scraper && uv pip install -e ".[dev]" --upgrade && uv pip freeze | grep -v '^-e \|^xee-labs' > requirements-lock.txt
asyncpg==0.31.0
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.1
coverage==7.15.4
curl-cffi==0.16.0
iniconfig==2.3.0
lxml==6.1.1
packaging==26.3
pluggy==1.6.0
pycparser==3.0
pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
soupsieve==2.9.2
typing-extensions==4.16.0
